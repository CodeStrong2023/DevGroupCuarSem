services:
        app-csv:
                build:
                     context:
                     dockerfile: Dockerfile
                    volumes:
                       - .:/app
                    ports:
                        - '80:80'

CMD bash -c "while true; do sleep 1; done"